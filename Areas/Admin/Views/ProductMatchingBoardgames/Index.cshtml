@model IEnumerable<Board_game_sleeves.Models.ProductMatchingBoardgame>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BoardGame.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Product.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BoardGame.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Product.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProductMatchingBoardgameId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProductMatchingBoardgameId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProductMatchingBoardgameId })
        </td>
    </tr>
}

</table>
